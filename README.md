# Rails 5.0.x + webpack + React + Redux + Router Sample

simple todo list

- [reference] [React Official Tutorial](http://redux.js.org/docs/basics/)

# Setup

```
bundle install --path vendor/bundle
npm --prefix client install
```

# Start

```
bundle exec foreman start
```
